// Registers the Cropper.js custom elements on import and names the dynamically imported chunk after this file,
// keeping its hashed URL under application control instead of the vendor dist filename.
import 'cropperjs';
