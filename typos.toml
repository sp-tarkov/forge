[default]
locale = "en-ca"

[default.extend-words]
monolog = "monolog"
Monolog = "Monolog"
# The Anthropic Message Batches API uses the US spelling in its wire format.
canceled = "canceled"

[files]
extend-exclude = [
    "/public/vendor/",
    "/public/robots.txt",
    "/package-lock.json",
    "/artisan",
    "/pint.json",
    "/rector.php",
    "/phpunit.xml",
    "/composer.lock",
    "*.min.*",
    ".gitignore",
    "/storage/app/blocklists/",
]
