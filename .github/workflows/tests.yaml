name: Tests

on: [ push, pull_request ]

permissions:
  contents: read

env:
  COMPOSER_AUTH: '{"http-basic": {"composer.fluxui.dev": {"username": "${{ secrets.FLUX_USERNAME }}", "password": "${{ secrets.FLUX_LICENSE_KEY }}"}}}'

jobs:
  # Builds the front-end assets a single time and shares them with every downstream test job. Both the feature
  # shards and the browser job need the Vite manifest, so building once and downloading the artifact avoids
  # rebuilding the same bundle on every runner.
  build-assets:
    runs-on: ubuntu-latest

    name: Build Assets

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: ./.github/action/php
        with:
          php-version: 8.5

      - name: Install Composer Dependencies
        run: composer install -q --no-interaction --no-progress --prefer-dist --optimize-autoloader

      - name: Setup Node.js
        uses: ./.github/action/node

      - name: Install NPM Dependencies
        run: npm ci

      - name: Prepare Environment
        run: php -r "file_exists('.env') || copy('.env.ci', '.env');"

      - name: Build Front-end Assets
        run: npm run build

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v7
        with:
          name: build-assets
          path: public/build
          retention-days: 1

  # The lightweight feature/unit shards. These never touch a browser, so they skip Playwright, Chromium, Reverb,
  # and Node entirely: they only need PHP, Composer, a database, and the prebuilt asset manifest. Every shard runs
  # twice, once against PostgreSQL (production) and once against SQLite (local development).
  feature:
    runs-on: ubuntu-latest

    name: Feature (${{ matrix.driver }}, Shard ${{ matrix.shard }}/4)

    needs: build-assets

    strategy:
      fail-fast: false
      matrix:
        driver: [ pgsql, sqlite ]
        shard: [ 1, 2, 3, 4 ]

    env:
      DB_CONNECTION: ${{ matrix.driver }}
      DB_HOST: 127.0.0.1
      DB_PORT: 5432
      DB_DATABASE: ${{ matrix.driver == 'sqlite' && 'database/testing.sqlite' || 'testing' }}
      DB_USERNAME: postgres
      DB_PASSWORD: password

    # GitHub Actions cannot attach a service to only part of a matrix, so the SQLite shards start this container
    # and then ignore it.
    services:
      postgres:
        image: postgres:18
        ports:
          - 5432:5432
        env:
          POSTGRES_DB: testing
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        options: --health-cmd=pg_isready --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: ./.github/action/php
        with:
          php-version: 8.5

      - name: Install Composer Dependencies
        run: composer install -q --no-interaction --no-progress --prefer-dist --optimize-autoloader

      - name: Download Build Artifact
        uses: actions/download-artifact@v8
        with:
          name: build-assets
          path: public/build

      - name: Prepare Laravel Environment
        run: |
          php -r "file_exists('.env') || copy('.env.ci', '.env');"
          php artisan key:generate
          php artisan optimize

      - name: Create SQLite Database File
        if: matrix.driver == 'sqlite'
        run: touch database/testing.sqlite

      - name: Run Database Migrations
        run: php artisan migrate

      - name: Link Storage
        run: php artisan storage:link

      - name: Run Tests
        run: ./vendor/bin/pest --ci --no-tia --parallel --testsuite=Feature,Unit --shard=${{ matrix.shard }}/4

      - name: Display Laravel Log
        if: failure()
        run: cat storage/logs/laravel.log

  # The heavyweight browser job. This is the only place that pays the Playwright/Chromium install cost and runs
  # Reverb, because only the browser suite drives a real browser against the served application. It runs against
  # PostgreSQL alone; the feature matrix is what covers the driver differences.
  browser:
    runs-on: ubuntu-latest

    name: Browser

    needs: build-assets

    env:
      BROADCAST_CONNECTION: reverb
      DB_CONNECTION: pgsql
      DB_HOST: 127.0.0.1
      DB_PORT: 5432
      DB_DATABASE: testing
      DB_USERNAME: postgres
      DB_PASSWORD: password

    services:
      postgres:
        image: postgres:18
        ports:
          - 5432:5432
        env:
          POSTGRES_DB: testing
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        options: --health-cmd=pg_isready --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: ./.github/action/php
        with:
          php-version: 8.5

      - name: Setup Node.js
        uses: ./.github/action/node

      - name: Install Composer Dependencies
        run: composer install -q --no-interaction --no-progress --prefer-dist --optimize-autoloader

      - name: Install NPM Dependencies
        run: npm ci

      - name: Download Build Artifact
        uses: actions/download-artifact@v8
        with:
          name: build-assets
          path: public/build

      - name: Cache Playwright Browsers
        id: playwright-cache
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright Browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Install Playwright System Dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: Prepare Laravel Environment
        run: |
          php -r "file_exists('.env') || copy('.env.ci', '.env');"
          php artisan key:generate
          php artisan optimize

      - name: Start Reverb Server
        run: php artisan reverb:start --host=127.0.0.1 --port=8080 &

      - name: Run Database Migrations
        run: php artisan migrate

      - name: Link Storage
        run: php artisan storage:link

      - name: Run Tests
        run: ./vendor/bin/pest --ci --no-tia --parallel --testsuite=Browser

      - name: Upload Screenshots
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: screenshots
          path: tests/Browser/screenshots

      - name: Display Laravel Log
        if: failure()
        run: cat storage/logs/laravel.log

  tests-complete:
    runs-on: ubuntu-latest

    name: Tests Complete

    needs: [ feature, browser ]

    if: ${{ always() }}

    steps:
      - name: Verify All Test Jobs Passed
        if: ${{ needs.feature.result != 'success' || needs.browser.result != 'success' }}
        run: |
          echo "Feature: ${{ needs.feature.result }} | Browser: ${{ needs.browser.result }}"
          exit 1

  type-coverage:
    runs-on: ubuntu-latest

    name: Type Coverage

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: ./.github/action/php
        with:
          php-version: 8.5

      - name: Install Composer Dependencies
        run: composer install -q --no-interaction --no-progress --prefer-dist --optimize-autoloader

      - name: Prepare Laravel Environment
        run: |
          php -r "file_exists('.env') || copy('.env.ci', '.env');"
          php artisan key:generate
          php artisan optimize

      - name: Run Type Coverage
        env:
          FORK_MEM_PER_PROC: "1099511627776" # 1 TiB per-process budget forces Pokio to one fork worker on any runner.
        run: ./vendor/bin/pest --type-coverage --min=100

      - name: Display Laravel Log
        if: failure()
        run: cat storage/logs/laravel.log
